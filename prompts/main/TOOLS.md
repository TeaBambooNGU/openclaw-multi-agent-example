# TOOLS.md - Local Notes (Sanitized)

## Workspace
- 主 workspace：`<MAIN_WORKSPACE>`
- code-dev workspace：`<CODE_WORKSPACE>`
- deep-researcher workspace：`<RESEARCH_WORKSPACE>`

## Local CLIs
- `node`：`<NODE_PATH>`
- `npm`：`<NPM_PATH>`
- `yarn`：`<YARN_PATH>`
- `pnpm`：已安装
- `codex`：`<CODEX_PATH>`
- `agent-browser`：`<AGENT_BROWSER_PATH>`

## Telegram / Sessions
- 当前主群 chat id：`telegram:<GROUP_CHAT_ID>`
- 委派默认使用专用 session key，而不是长期复用固定会话。
